{
  "type": "module",
  "private": true,
  "dependencies": {
    "@observablehq/notebook-kit": "^1.2.0"
  },
  "scripts": {
    "dev": "notebooks preview --root notebooks",
    "build": "notebooks build --root notebooks --out ../output -- notebooks/analysis.html"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}