{
  "name": "@samizdata/{{ cookiecutter.project_slug }}-visuals",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "prepare:data": "node scripts/prepare-data.mjs",
    "build": "pnpm prepare:data && vite build"
  },
  "dependencies": {
    "@samizdata/graphics": "{{ cookiecutter.graphics_package }}",
    "layercake": "^10.0.3",
    "svelte": "^5.56.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@types/node": "^24.0.0",
    "svelte-check": "^4.6.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.3.0"
}
